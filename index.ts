// index.js
import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent ensures that your App is registered correctly
// and works both in Expo Go and in a standalone native build
registerRootComponent(App);
